/**
 * Health probe endpoint path constants.
 * Used across all services for consistent endpoint routing.
 */

export const HEALTH_LIVE_PATH = '/health/live';
export const HEALTH_READY_PATH = '/health/ready';
